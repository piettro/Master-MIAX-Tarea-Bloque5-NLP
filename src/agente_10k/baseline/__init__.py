"""La implementación del profesor, preservada TAL CUAL. No se modifica.

Es el BASELINE contra el que se compara el sistema final y media tabla del
informe. `miax_s1.py` y `demo_traza.json` son copias literales del material de
la sesión 1; `ruff` y `mypy` los excluyen a propósito (ver pyproject.toml) para
que nadie los "arregle" sin darse cuenta.

El protocolo de congelación está en `resultados/README.md`.
"""

from __future__ import annotations
